# To-Find-Factorial
The statement factorial = factorial * i is used inside a loop to calculate the factorial of a number. It multiplies the current value of factorial by each number from 1 to n, building the final result step by step.
